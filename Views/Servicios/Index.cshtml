
@{
    ViewBag.Title = "Servicios";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    List<Proyecto_Web_Ingenieria_de_Software.Models.Services> services = ViewBag.Servicios;
}

<link rel="stylesheet" href="~/Content/styleUsuarios.css" />

<div class="serviciosDiv mb-5 p-5">
    <div class="row">
        <div class="col-8">
            <h1 class="t-stroke">Servicios</h1>
        </div>
        <div class="col-4 d-flex justify-content-end">
            <button class="btn btn-success" onclick="document.location.href = '@Url.Content("~/Servicios/Agregar")'">Nuevo Servicio</button>
        </div>
    </div>
    <hr class="text-black-50" />
    <div class="table-responsive">
        <table class="table table-hover table-sm">
            <thead>
                <tr>
                    <th scope="col">Codigo</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Descripción</th>
                    <th scope="col">Precio</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var s in services)
                {
                    <tr>
                        <td>@s.ServiceCode</td>
                        <td>@s.ServiceName</td>
                        <td>@s.Description</td>
                        <td>@s.PrecioTotal</td>
                        <td>
                            <button class="btn btn-sm btn-primary" onclick="document.location.href = '@Url.Content("~/Servicios/Editar/")' + @s.ID">Editar</button>
                        </td>
                    </tr>
                  }
            </tbody>
        </table>
    </div>
</div>

